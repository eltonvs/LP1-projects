// Copyright - 2016

#include "queue.hpp"

template <typename Data>
Queue<Data>::Queue(const int _SIZE) {
    SIZE = _SIZE;
}

template <typename Data>
bool Queue<Data>::enqueue(const Data _a) {
    if (f == -1) {
        // bla
    }
    return true;
}
